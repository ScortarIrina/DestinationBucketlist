package com.example.destinationbucketlist.repository;

import com.example.destinationbucketlist.model.PrivateDestination;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PrivateDestinationRepository extends JpaRepository<PrivateDestination, Integer> {
}
